Para inicar el Backend
Avance Final/backend/
--npm install (si no as instalando todas las dependencias)
--npm run dev (para iniciar el servidor)


Para Inicar el Frontend
Avance Final/frontend/
--npm install (si no as instalando todas las dependencias)
--npm run dev (para iniciar el servidor)

